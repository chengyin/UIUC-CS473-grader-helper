A helper script written for CS473 grading process to avoid renaming each file by hand.

The script will search the current folder for all files with name that "looks like" a homework and open it with the DEFAULT pdf viewer. After grading, the grader can input the points for the homework and the renaming process is automatic.